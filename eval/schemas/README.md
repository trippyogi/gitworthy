# Eval JSON Schema sources

Machine-readable schemas for eval cases and provider fixtures are generated into
repo-root `schemas/` via `pnpm schemas:generate` from:

- `src/contracts/eval.ts`
- `src/contracts/provider-fixtures.ts`

This directory documents the boundary called out in GW-021 (`eval/schemas`).
