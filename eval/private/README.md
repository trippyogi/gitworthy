# Private evaluation (gitignored)

Local capture experiments only. Contents of this directory are ignored by git except this README.

Private cases:

- Must declare `suite: "private"`
- Must **not** use `classification: "frozen"`
- Become release blockers only after explicit promotion into `eval/frozen/`
