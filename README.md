# gitworthy

is it worth your commit?

Phase one scaffold is in place. The CLI help path and temporary MCP echo tool verify the package wiring before core checks are added.

No telemetry is active by default.
